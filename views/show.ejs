<!-- <!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>show</title>
    <link href="/style.css" rel="stylesheet">
</head>

<body>
    <div>
        <h2>
            Id:<%=post.id%>
        </h2>
        <h3>@<%=post.username%>
        </h3>
        <h4>content : <%= post.content%>
        </h4>
    </div>
    <button><a href="http://localhost:8080/posts">Goback!</a></button>
</body>

</html> -->
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Post #<%= post.id %> • Just Say It</title>

    <link href="/style.css" rel="stylesheet">
</head>

<body>

    <div class="container">

        <!-- Back -->
        <a href="/posts" class="back-btn">
            ← Back to posts
        </a>


        <!-- Post Detail -->
        <div class="show-card">

            <div class="show-header">

                <div class="avatar">
                    <%= post.username.charAt(0).toUpperCase() %>
                </div>

                <div>
                    <p class="show-label">POST #<%= post.id %>
                    </p>

                    <h2>
                        @<%= post.username %>
                    </h2>
                </div>

            </div>


            <div class="show-content">
                <p>
                    <%= post.content %>
                </p>
            </div>


            <div class="show-footer">

                <span>
                    Just Say It.
                </span>

                <a href="/posts" class="action view">
                    View all posts
                </a>

            </div>

        </div>

    </div>
    <!-- your existing HTML -->

    <div class="theme-toggle">
        <button id="themeToggle" type="button">
            🌙
        </button>
    </div>

    <script>
        const themeButton = document.getElementById("themeToggle");

        if (localStorage.getItem("theme") === "dark") {
            document.body.classList.add("dark-mode");
            themeButton.textContent = "☀️";
        }

        themeButton.addEventListener("click", function () {

            document.body.classList.toggle("dark-mode");

            if (document.body.classList.contains("dark-mode")) {
                localStorage.setItem("theme", "dark");
                themeButton.textContent = "☀️";
            } else {
                localStorage.setItem("theme", "light");
                themeButton.textContent = "🌙";
            }

        });
    </script>

</body>

</html>


</body>

</html>